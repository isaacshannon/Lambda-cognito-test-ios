# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'lambda-testeroo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for lambda-testeroo
    pod 'AWSCognito'
    pod 'AWSCore'
    pod 'AWSLambda'

  target 'lambda-testerooTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'lambda-testerooUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
